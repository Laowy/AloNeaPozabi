<UserControl x:Class="AloNeaPozabi.TaskControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" Background="AntiqueWhite">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Row="0" Content="is finished?" />
        <Button  Grid.Row="0" Grid.Column="1" Content="delete tesk" />
        <TextBlock Grid.ColumnSpan="2" Grid.Row="1"  Text="ime opravila" />
        <TextBlock Grid.ColumnSpan="2"  Grid.Row="2" Text="rok opravila" />
        <TextBlock  Grid.ColumnSpan="2" Grid.Row="3" Text="ostali cas opravila" />
        <ProgressBar  Grid.ColumnSpan="2" Grid.Row="4" Minimum="0" Maximum="100" Value="20" />
    </Grid>
</UserControl>