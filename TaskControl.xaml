<UserControl x:Class="AloNeaPozabi.TaskControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" Background="AntiqueWhite">
    <Grid Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CheckBox x:Name="CheckboxIsCompleted" Grid.Row="0" Content="Completed?" />
        <TextBlock x:Name="TextBlockJobName" Grid.Row="1"  Text="ime opravila" />
        <TextBlock x:Name="TextBlockDueDate"   Grid.Row="2" Text="rok opravila" />

        <Button x:Name="ButtonRemoveTask"  Grid.Row="3"  Content="Delete task" />
    </Grid>
</UserControl>